# Dependencies
node_modules/
mcp-server/node_modules/
mcpwp/vendor/
mcpwp/composer.lock

# Build output
mcp-server/dist/
dist/

# Environment
.env
.env.local
.mcp.json
userpassword
userpassword.*
credentials
credentials.*

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Plugin zip (built artifacts)
plugin/*.zip
mcpwp/scripts/*.zip

# Local release artifacts (repo root)
mcpwp-*.zip
mcpwp-premium-*.zip
mcpwp-pro-*.zip

# Local tooling scratch (keep the real release script)
scripts/*.py
scripts/*.php
scripts/*.sh
!scripts/release_freemius.sh
!scripts/site-sync.sh
!scripts/build-wporg.sh
!scripts/publish_update_release.sh

# Test caches
mcpwp/tests/.phpunit.result.cache
.claude/
# but track the mumcp squad's agent definitions — they belong to the MCPWP tenant's repo
!.claude/
!.claude/agents/
!.claude/agents/*.md
mcp-server/*.mcpb
.claude/worktrees/
site-pilot-ai/
